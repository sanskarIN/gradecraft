{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-export",
  "description": "Allow the local GradeCraft main window to show a native save dialog and write only user-selected export files on desktop.",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": ["dialog:default", "fs:write-files"]
}
