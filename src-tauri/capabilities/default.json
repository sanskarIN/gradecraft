{
  "identifier": "default",
  "description": "Shared least-privilege core capability for the local GradeCraft main window on every native target.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
