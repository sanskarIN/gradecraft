import type { AppData, Assignment, Category, Course, GradeBand, GradeScaleProfile, Settings } from "../domain/types";
function isRecord(value:unknown):value is Record<string,unknown>{return typeof value==="object"&&value!==null&&!Array.isArray(value);}
function isFiniteNumber(value:unknown):value is number{return typeof value==="number"&&Number.isFinite(value);}
function isString(value:unknown):value is string{return typeof value==="string";}
function isCategory(value:unknown):value is Category{if(!isRecord(value))return false;return isString(value.id)&&isString(value.name)&&isFiniteNumber(value.weight)&&value.weight>=0&&value.weight<=100;}
function isAssignment(value:unknown):value is Assignment{if(!isRecord(value))return false;return isString(value.id)&&isString(value.name)&&isString(value.categoryId)&&isFiniteNumber(value.score)&&value.score>=0&&isFiniteNumber(value.maxScore)&&value.maxScore>0&&value.score<=value.maxScore&&(value.dueDate===undefined||isString(value.dueDate))&&isString(value.createdAt)&&isString(value.updatedAt);}
function isGradeBand(value:unknown):value is GradeBand{if(!isRecord(value))return false;return isString(value.id)&&isString(value.label)&&isFiniteNumber(value.minPercent)&&value.minPercent>=0&&value.minPercent<=100&&isFiniteNumber(value.gpaPoints)&&value.gpaPoints>=0&&value.gpaPoints<=10;}
function isGradeScale(value:unknown):value is GradeScaleProfile{if(!isRecord(value)||!isString(value.id)||!isString(value.name)||!Array.isArray(value.bands))return false;return value.bands.length>0&&value.bands.every(isGradeBand);}
function isCourse(value:unknown):value is Course{if(!isRecord(value))return false;if(!isString(value.id)||!isString(value.name)||!isString(value.code)||(value.semester!==undefined&&(!isString(value.semester)||value.semester.length>60))||!isString(value.color)||!isFiniteNumber(value.creditHours)||value.creditHours<0||value.creditHours>30||(value.mode!=="weighted"&&value.mode!=="points")||!isString(value.scaleId)||!Array.isArray(value.categories)||!value.categories.every(isCategory)||!Array.isArray(value.assignments)||!value.assignments.every(isAssignment)||!isString(value.createdAt)||!isString(value.updatedAt))return false;const categoryIds=new Set(value.categories.map((category)=>category.id));if(categoryIds.size!==value.categories.length)return false;if(!value.assignments.every((assignment)=>categoryIds.has(assignment.categoryId)))return false;if(value.mode==="weighted"&&value.categories.length>0){const total=value.categories.reduce((sum,category)=>sum+category.weight,0);if(Math.abs(total-100)>0.01)return false;}return true;}
function isSettings(value:unknown):value is Settings{if(!isRecord(value))return false;return(value.theme==="light"||value.theme==="dark"||value.theme==="system")&&(value.language===undefined||value.language==="en"||value.language==="hi")&&typeof value.reducedMotion==="boolean"&&typeof value.compactMode==="boolean"&&typeof value.onboardingComplete==="boolean";}
export function isAppData(value:unknown):value is AppData{if(!isRecord(value))return false;if(value.schemaVersion!==1||!Array.isArray(value.courses)||!value.courses.every(isCourse)||!Array.isArray(value.gradeScales)||!value.gradeScales.every(isGradeScale)||!isSettings(value.settings)||!isString(value.lastUpdatedAt))return false;const scaleIds=new Set(value.gradeScales.map((scale)=>scale.id));return scaleIds.size===value.gradeScales.length&&value.courses.every((course)=>scaleIds.has(course.scaleId));}
