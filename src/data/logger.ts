type LogLevel="info"|"warn"|"error";type Context=Record<string,unknown>;const SENSITIVE_KEY=/(password|token|secret|authorization|cookie|email|name|student|course|assignment|category|grade|score)/i,EMAIL_VALUE=/\b[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}\b/gi,BEARER_VALUE=/\bBearer\s+[A-Za-z0-9._~+/-]+=*\b/gi;
function redactString(value:string):string{return value.replace(EMAIL_VALUE,"[REDACTED_EMAIL]").replace(BEARER_VALUE,"[REDACTED_TOKEN]");}
function redact(value:unknown):unknown{if(typeof value==="string")return redactString(value);if(Array.isArray(value))return value.map(redact);if(value&&typeof value==="object")return Object.fromEntries(Object.entries(value as Record<string,unknown>).map(([k,v])=>[k,SENSITIVE_KEY.test(k)?"[REDACTED]":redact(v)]));return value;}
export function log(level:LogLevel,event:string,context:Context={}):void{const payload={event,context:redact(context),at:new Date().toISOString()};if(level==="error")console.error(payload);else if(level==="warn")console.warn(payload);else console.info(payload);}
