import { useEffect,useRef,type PropsWithChildren } from "react";import { en } from "../i18n/en";import { Button } from "./Button";
export function Modal({title,open,onClose,children}:PropsWithChildren<{title:string;open:boolean;onClose:()=>void}>){const ref=useRef<HTMLDialogElement>(null);useEffect(()=>{const d=ref.current;if(!d)return;if(open&&!d.open)d.showModal();if(!open&&d.open)d.close();},[open]);return <dialog ref={ref} className="modal" onCancel={(e)=>{e.preventDefault();onClose();}} onClose={()=>{if(open)onClose();}}><div className="modal__header"><h2>{title}</h2><Button variant="ghost" onClick={onClose} aria-label={en.closeDialog}>×</Button></div>{open?children:null}</dialog>;}
