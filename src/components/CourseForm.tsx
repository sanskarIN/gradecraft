import { useMemo,useState,type FormEvent } from "react";
import type { Course,CourseMode,GradeScaleProfile } from "../domain/types";import { validateCategories } from "../domain/validation";import { createId } from "../utils/id";import { Button } from "./Button";
const COLORS=["#4f46e5","#0f766e","#b45309","#be123c","#6d28d9","#0369a1"];
export function CourseForm({scales,initial,onSave,onCancel}:{scales:GradeScaleProfile[];initial?:Course;onSave:(course:Course)=>void;onCancel?:()=>void}){const[name,setName]=useState(initial?.name??""),[code,setCode]=useState(initial?.code??""),[creditHours,setCreditHours]=useState(initial?.creditHours??3),[mode,setMode]=useState<CourseMode>(initial?.mode??"weighted"),[scaleId,setScaleId]=useState(initial?.scaleId??scales[0]?.id??""),[categories,setCategories]=useState(initial?.categories??[{id:createId("category"),name:"Assignments",weight:40},{id:createId("category"),name:"Tests",weight:60}]);const issues=useMemo(()=>validateCategories(categories,mode),[categories,mode]),creditHoursValid=Number.isFinite(creditHours)&&creditHours>=0&&creditHours<=30,canSave=name.trim().length>0&&issues.length===0&&Boolean(scaleId)&&creditHoursValid;
function submit(event:FormEvent){event.preventDefault();if(!canSave)return;const now=new Date().toISOString();onSave({id:initial?.id??createId("course"),name:name.trim(),code:code.trim(),color:initial?.color??COLORS[Math.floor(Math.random()*COLORS.length)]??COLORS[0]!,creditHours,mode,scaleId,categories,assignments:initial?.assignments??[],createdAt:initial?.createdAt??now,updatedAt:now});}
return <form className="form-stack" onSubmit={submit}><div className="form-grid"><label>Course name<input value={name} onChange={(e)=>setName(e.target.value)} required maxLength={80}/></label><label>Course code<input value={code} onChange={(e)=>setCode(e.target.value)} maxLength={24} placeholder="e.g. MATH 101"/></label><label>Credit hours<input type="number" min="0" max="30" step="0.5" value={creditHours} onChange={(e)=>setCreditHours(Number(e.target.value))}/>{!creditHoursValid&&<small className="field-error">Credit hours must be between 0 and 30.</small>}</label><label>Calculation mode<select value={mode} onChange={(e)=>setMode(e.target.value as CourseMode)}><option value="weighted">Weighted categories</option><option value="points">Unweighted points</option></select></label><label>Grading scale<select value={scaleId} onChange={(e)=>setScaleId(e.target.value)} required>{scales.map((scale)=><option key={scale.id} value={scale.id}>{scale.name}</option>)}</select></label></div><fieldset><legend>Categories</legend><p className="help">{mode==="weighted"?"Weights must total exactly 100%.":"Weights are ignored in points mode."}</p><div className="category-editor">{categories.map((category,index)=><div className="category-editor__row" key={category.id}><label>Category {index+1}<input value={category.name} onChange={(e)=>setCategories((items)=>items.map((item)=>item.id===category.id?{...item,name:e.target.value}:item))}/></label><label>Weight %<input type="number" min="0" max="100" step="0.01" value={category.weight} disabled={mode==="points"} onChange={(e)=>setCategories((items)=>items.map((item)=>item.id===category.id?{...item,weight:Number(e.target.value)}:item))}/></label><Button type="button" variant="ghost" onClick={()=>setCategories((items)=>items.filter((item)=>item.id!==category.id))} disabled={categories.length<=1||Boolean(initial?.assignments.some((assignment)=>assignment.categoryId===category.id))} title={initial?.assignments.some((assignment)=>assignment.categoryId===category.id)?"Move or delete assignments in this category before removing it.":undefined}>Remove</Button></div>)}</div><Button type="button" variant="secondary" onClick={()=>setCategories((items)=>[...items,{id:createId("category"),name:"",weight:0}])}>Add category</Button>{issues.length>0&&<div className="validation-summary" role="alert">{issues.map((issue)=><p key={`${issue.field}-${issue.message}`}>{issue.message}</p>)}</div>}</fieldset><div className="form-actions">{onCancel&&<Button type="button" variant="secondary" onClick={onCancel}>Cancel</Button>}<Button type="submit" disabled={!canSave}>Save course</Button></div></form>;}
