import { useState } from "react";import { useApp } from "../state/AppContext";import { Button } from "./Button";
const steps=[{title:"Welcome to GradeCraft",body:"Track grades, test scenarios, and calculate GPA without creating an account."},{title:"Private by default",body:"Your courses are stored locally in this browser. Export a backup whenever you want portability."},{title:"Built for real grading systems",body:"Choose weighted categories or point-based grading, then customize scales and credits."}];
export function Onboarding(){const{dispatch}=useApp();const[step,setStep]=useState(0),current=steps[step]!;return <div className="onboarding" role="dialog" aria-modal="true" aria-labelledby="onboarding-title"><div className="onboarding__panel"><span className="brand__mark onboarding__mark">G</span><p className="eyebrow">Step {step+1} of {steps.length}</p><h1 id="onboarding-title">{current.title}</h1><p>{current.body}</p><div className="onboarding__dots" aria-hidden="true">{steps.map((_,index)=><span key={index} className={index===step?"is-active":""}/>)}</div><div className="form-actions">{step>0&&<Button variant="secondary" onClick={()=>setStep((v)=>v-1)}>Back</Button>}<Button onClick={()=>{if(step<steps.length-1)setStep((v)=>v+1);else dispatch({type:"settings/update",settings:{onboardingComplete:true}});}}>{step===steps.length-1?"Start using GradeCraft":"Continue"}</Button></div></div></div>;}
