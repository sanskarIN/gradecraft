import type { CategoryResult } from "../domain/types";
export function ContributionChart({results,names}:{results:CategoryResult[];names:Record<string,string>}){return <div className="bars" aria-label="Category contribution chart">{results.map((r)=><div className="bar-row" key={r.categoryId}><div className="bar-row__label"><span>{names[r.categoryId]??"Category"}</span><span>{r.percent===null?"No grades":`${r.percent.toFixed(1)}%`}</span></div><div className="bar-track"><span style={{width:`${Math.min(100,Math.max(0,r.percent??0))}%`}}/></div><small>{r.weight}% course weight · {r.contribution.toFixed(1)} pts contributed</small></div>)}</div>;}
