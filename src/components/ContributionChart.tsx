import type { CategoryResult } from "../domain/types";import { en } from "../i18n/en";
export function ContributionChart({results,names}:{results:CategoryResult[];names:Record<string,string>}){return <div className="bars" role="img" aria-label={en.charts.contributionLabel}>{results.map((r)=><div className="bar-row" key={r.categoryId}><div className="bar-row__label"><span>{names[r.categoryId]??en.charts.category}</span><span>{r.percent===null?en.charts.noGrades:`${r.percent.toFixed(1)}%`}</span></div><div className="bar-track" aria-hidden="true"><span style={{width:`${Math.min(100,Math.max(0,r.percent??0))}%`}}/></div><small>{en.charts.contribution(r.weight,r.contribution)}</small></div>)}</div>;}
