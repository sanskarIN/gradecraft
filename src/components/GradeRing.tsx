export function GradeRing({value,label="Current grade"}:{value:number|null;label?:string}){const safe=value===null?0:Math.min(100,Math.max(0,value)),radius=52,c=2*Math.PI*radius,offset=c-(safe/100)*c;return <figure className="grade-ring" aria-label={`${label}: ${value===null?"not available":`${value.toFixed(1)} percent`}`}><svg viewBox="0 0 128 128" aria-hidden="true"><circle className="grade-ring__track" cx="64" cy="64" r={radius}/><circle className="grade-ring__value" cx="64" cy="64" r={radius} strokeDasharray={c} strokeDashoffset={offset}/></svg><figcaption><strong>{value===null?"—":`${value.toFixed(1)}%`}</strong><span>{label}</span></figcaption></figure>;}
