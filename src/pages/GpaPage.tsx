import { calculateGpa } from "../domain/gpa";import { calculateCourseGrade,gradeBandForPercent } from "../domain/gradeMath";import { useApp } from "../state/AppContext";import { Card } from "../components/Card";import { EmptyState } from "../components/EmptyState";
export function GpaPage(){const{data}=useApp(),result=calculateGpa(data.courses,data.gradeScales);if(data.courses.length===0)return <EmptyState title="No GPA data yet" description="Add courses and grades to calculate a credit-weighted GPA."/>;return <div className="page-stack"><div className="page-heading"><div><p className="eyebrow">Academic overview</p><h1>GPA calculator</h1><p>Uses each course's selected grading scale and credit hours.</p></div></div><div className="hero-grid"><Card title="Current GPA"><p className="giant-number">{result.gpa===null?"—":result.gpa.toFixed(2)}</p><p>{result.attemptedCredits} attempted credits · {result.qualityPoints.toFixed(2)} quality points</p></Card><Card title="Scale profiles"><p>{data.gradeScales.length} local profile{data.gradeScales.length===1?"":"s"}. Configure scale bands from Settings.</p></Card></div><Card title="Course breakdown"><div className="table-wrap"><table><thead><tr><th>Course</th><th>Grade</th><th>Letter</th><th>Credits</th><th>GPA points</th></tr></thead><tbody>{data.courses.map((course)=>{const percent=calculateCourseGrade(course).percent,scale=data.gradeScales.find((item)=>item.id===course.scaleId),band=scale?gradeBandForPercent(percent,scale):null,courseResult=result.courses.find((item)=>item.courseId===course.id);return <tr key={course.id}><td>{course.name}</td><td>{percent===null?"—":`${percent.toFixed(1)}%`}</td><td>{band?.label??"—"}</td><td>{course.creditHours}</td><td>{courseResult?.points??"—"}</td></tr>;})}</tbody></table></div></Card></div>;}
