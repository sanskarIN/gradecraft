import { calculateGpa } from "../domain/gpa";import { calculateCourseGrade,gradeBandForPercent } from "../domain/gradeMath";import { useApp } from "../state/AppContext";import { Card } from "../components/Card";import { EmptyState } from "../components/EmptyState";import { en } from "../i18n/en";
export function GpaPage(){const{data}=useApp(),result=calculateGpa(data.courses,data.gradeScales);if(data.courses.length===0)return <EmptyState title={en.gpa.emptyTitle} description={en.gpa.emptyDescription}/>;return <div className="page-stack"><div className="page-heading"><div><p className="eyebrow">{en.gpa.eyebrow}</p><h1>{en.gpa.title}</h1><p>{en.gpa.description}</p></div></div><div className="hero-grid"><Card title={en.gpa.currentGpa}><p className="giant-number">{result.gpa===null?"—":result.gpa.toFixed(2)}</p><p>{en.gpa.summary(result.attemptedCredits,result.qualityPoints)}</p></Card><Card title={en.gpa.scaleProfiles}><p>{en.gpa.profiles(data.gradeScales.length)}</p></Card></div><Card title={en.gpa.breakdown}><div className="table-wrap"><table><thead><tr><th>{en.gpa.course}</th><th>{en.gpa.grade}</th><th>{en.gpa.letter}</th><th>{en.gpa.credits}</th><th>{en.gpa.gpaPoints}</th></tr></thead><tbody>{data.courses.map((course)=>{const percent=calculateCourseGrade(course).percent,scale=data.gradeScales.find((item)=>item.id===course.scaleId),band=scale?gradeBandForPercent(percent,scale):null,courseResult=result.courses.find((item)=>item.courseId===course.id);return <tr key={course.id}><td>{course.name}</td><td>{percent===null?"—":`${percent.toFixed(1)}%`}</td><td>{band?.label??"—"}</td><td>{course.creditHours}</td><td>{courseResult?.points??"—"}</td></tr>;})}</tbody></table></div></Card></div>;}
