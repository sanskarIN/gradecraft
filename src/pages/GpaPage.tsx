import { calculateGpa } from "../domain/gpa";import { calculateCourseGrade,gradeBandForPercent } from "../domain/gradeMath";import { useMessages } from "../i18n/useMessages";import { useApp } from "../state/AppContext";import { Card } from "../components/Card";import { EmptyState } from "../components/EmptyState";
export function GpaPage(){const{data}=useApp(),messages=useMessages(),result=calculateGpa(data.courses,data.gradeScales);if(data.courses.length===0)return <EmptyState title={messages.gpaEmptyTitle} description={messages.gpaEmptyHint}/>;return <div className="page-stack"><div className="page-heading"><div><p className="eyebrow">{messages.gpaEyebrow}</p><h1>{messages.gpaTitle}</h1><p>{messages.gpaIntro}</p></div></div><div className="hero-grid"><Card title={messages.currentGpa}><p className="giant-number">{result.gpa===null?"—":result.gpa.toFixed(2)}</p><p>{messages.gpaSummary(result.attemptedCredits,result.qualityPoints)}</p></Card><Card title={messages.scaleProfiles}><p>{messages.scaleProfilesSummary(data.gradeScales.length)}</p></Card></div><Card title={messages.courseBreakdown}><div className="table-wrap"><table><thead><tr><th>{messages.course}</th><th>{messages.grade}</th><th>{messages.letter}</th><th>{messages.credits}</th><th>{messages.gpaPoints}</th></tr></thead><tbody>{data.courses.map((course)=>{const percent=calculateCourseGrade(course).percent,scale=data.gradeScales.find((item)=>item.id===course.scaleId),band=scale?gradeBandForPercent(percent,scale):null,courseResult=result.courses.find((item)=>item.courseId===course.id);return <tr key={course.id}><td>{course.name}</td><td>{percent===null?"—":`${percent.toFixed(1)}%`}</td><td>{band?.label??"—"}</td><td>{course.creditHours}</td><td>{courseResult?.points??"—"}</td></tr>;})}</tbody></table></div></Card></div>;}
