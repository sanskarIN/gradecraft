import { useState } from "react";
import { calculateCourseGrade,gradeBandForPercent } from "../domain/gradeMath";import type { Assignment } from "../domain/types";import { useMessages } from "../i18n/useMessages";import { useApp } from "../state/AppContext";import { AssignmentForm } from "../components/AssignmentForm";import { Button } from "../components/Button";import { Card } from "../components/Card";import { ContributionChart } from "../components/ContributionChart";import { CourseForm } from "../components/CourseForm";import { EmptyState } from "../components/EmptyState";import { GradeRing } from "../components/GradeRing";import { Modal } from "../components/Modal";import { TrendChart } from "../components/TrendChart";import { navigate } from "../hooks/useHashRoute";
export function CoursePage({id}:{id:string}){const{data,dispatch}=useApp(),messages=useMessages(),course=data.courses.find((item)=>item.id===id);const[assignmentOpen,setAssignmentOpen]=useState(false),[editingAssignment,setEditingAssignment]=useState<Assignment|null>(null),[editOpen,setEditOpen]=useState(false),[lastDeleted,setLastDeleted]=useState<Assignment|null>(null);if(!course)return <EmptyState title={messages.courseNotFound} description={messages.courseNotFoundHint} action={<Button onClick={()=>navigate("/dashboard")}>{messages.backCourses}</Button>}/>;const result=calculateCourseGrade(course),scale=data.gradeScales.find((item)=>item.id===course.scaleId),band=scale?gradeBandForPercent(result.percent,scale):null,categoryNames=Object.fromEntries(course.categories.map((category)=>[category.id,category.name]));function saveAssignment(assignment:Assignment){const exists=course!.assignments.some((item)=>item.id===assignment.id),assignments=exists?course!.assignments.map((item)=>item.id===assignment.id?assignment:item):[...course!.assignments,assignment];dispatch({type:"course/update",course:{...course!,assignments,updatedAt:new Date().toISOString()}});setAssignmentOpen(false);setEditingAssignment(null);}function deleteAssignment(assignment:Assignment){dispatch({type:"course/update",course:{...course!,assignments:course!.assignments.filter((item)=>item.id!==assignment.id),updatedAt:new Date().toISOString()}});setLastDeleted(assignment);}function undoDelete(){if(!lastDeleted)return;dispatch({type:"course/update",course:{...course!,assignments:[...course!.assignments,lastDeleted],updatedAt:new Date().toISOString()}});setLastDeleted(null);}return <div className="page-stack"><div className="page-heading"><div><button className="back-link" onClick={()=>navigate("/dashboard")}>← {messages.backCourses}</button><p className="eyebrow">{course.code||messages.courseFallback}</p><h1>{course.name}</h1><p>{course.mode==="weighted"?messages.weightedGrading:messages.pointsGrading}{course.semester?` · ${course.semester}`:""}</p></div><div className="button-row"><Button variant="secondary" onClick={()=>setEditOpen(true)}>{messages.editCourse}</Button><Button onClick={()=>setAssignmentOpen(true)}>{messages.addAssignment}</Button></div></div>{lastDeleted&&<div className="notice undo-notice" role="status"><span>{messages.deletedAssignment(lastDeleted.name)}</span><Button variant="secondary" onClick={undoDelete}>{messages.undo}</Button></div>}<div className="hero-grid"><Card><GradeRing value={result.percent} label={band?messages.gradeWithLabel(band.label):messages.currentGrade}/></Card><Card title={messages.gradeSummary}><dl className="stats"><div><dt>{messages.earnedPoints}</dt><dd>{result.earned.toFixed(1)}</dd></div><div><dt>{messages.possiblePoints}</dt><dd>{result.possible.toFixed(1)}</dd></div><div><dt>{messages.activeWeight}</dt><dd>{course.mode==="weighted"?`${result.activeWeight.toFixed(1)}%`:"N/A"}</dd></div><div><dt>{messages.scale}</dt><dd>{scale?.name??messages.unknown}</dd></div></dl></Card></div><Card title={messages.assignmentsTitle} actions={<Button variant="secondary" onClick={()=>navigate(`/what-if/${course.id}`)}>{messages.openWhatIf}</Button>}>{course.assignments.length===0?<EmptyState title={messages.noAssignments} description={messages.noAssignmentsHint}/>:<div className="table-wrap"><table><thead><tr><th>{messages.whatIfAssignment}</th><th>{messages.category}</th><th>{messages.score}</th><th>{messages.percent}</th><th>{messages.due}</th><th>{messages.actions}</th></tr></thead><tbody>{course.assignments.map((assignment)=>{const percent=(assignment.score/assignment.maxScore)*100;return <tr key={assignment.id}><td>{assignment.name}</td><td>{categoryNames[assignment.categoryId]}</td><td>{assignment.score} / {assignment.maxScore}</td><td>{percent.toFixed(1)}%</td><td>{assignment.dueDate??"—"}</td><td><div className="button-row"><Button variant="ghost" onClick={()=>setEditingAssignment(assignment)}>{messages.edit}</Button><Button variant="ghost" onClick={()=>deleteAssignment(assignment)}>{messages.delete}</Button></div></td></tr>;})}</tbody></table></div>}</Card><div className="two-column"><Card title={messages.scoreTrend}><TrendChart assignments={course.assignments}/></Card><Card title={messages.categoryContribution}><ContributionChart results={result.categoryResults} names={categoryNames}/></Card></div><Card title={messages.dangerZone}><p>{messages.deleteCourseHelp}</p><Button variant="danger" onClick={()=>{if(window.confirm(messages.deleteCourseConfirm(course.name))){dispatch({type:"course/delete",id:course.id});navigate("/dashboard");}}}>{messages.deleteCourse}</Button></Card><Modal title={messages.addAssignment} open={assignmentOpen} onClose={()=>setAssignmentOpen(false)}><AssignmentForm course={course} onSave={saveAssignment} onCancel={()=>setAssignmentOpen(false)}/></Modal><Modal title={messages.editAssignmentTitle} open={editingAssignment!==null} onClose={()=>setEditingAssignment(null)}>{editingAssignment&&<AssignmentForm course={course} initial={editingAssignment} onSave={saveAssignment} onCancel={()=>setEditingAssignment(null)}/>}</Modal><Modal title={messages.editCourseTitle} open={editOpen} onClose={()=>setEditOpen(false)}><CourseForm scales={data.gradeScales} initial={course} onCancel={()=>setEditOpen(false)} onSave={(updated)=>{dispatch({type:"course/update",course:updated});setEditOpen(false);}}/></Modal></div>;}
