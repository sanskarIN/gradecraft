import { useRef,useState } from "react";
import { createBackup,parseBackup } from "../data/backup";import { exportCourseCsv,importAssignmentsCsv } from "../data/csv";import { log } from "../data/logger";import type { Course } from "../domain/types";import { validateCategories } from "../domain/validation";import { useMessages } from "../i18n/useMessages";import { useApp } from "../state/AppContext";import { createId } from "../utils/id";import { downloadText } from "../utils/download";import { Button } from "../components/Button";import { Card } from "../components/Card";
export function DataPage(){const{data,dispatch}=useApp(),messages=useMessages();const[courseId,setCourseId]=useState(data.courses[0]?.id??""),[message,setMessage]=useState("");const backupInput=useRef<HTMLInputElement>(null),csvInput=useRef<HTMLInputElement>(null);async function readFile(file:File):Promise<string>{if(file.size>5_000_000)throw new Error("file-too-large");return file.text();}return <div className="page-stack"><div className="page-heading"><div><p className="eyebrow">{messages.dataEyebrow}</p><h1>{messages.dataTitle}</h1><p>{messages.dataIntro}</p></div></div>{message&&<div className="notice" role="status">{message}</div>}<div className="two-column"><Card title={messages.fullBackup} eyebrow={messages.jsonLabel}><p>{messages.fullBackupHelp}</p><div className="button-row"><Button onClick={()=>downloadText(`gradecraft-backup-${new Date().toISOString().slice(0,10)}.json`,createBackup(data),"application/json")}>{messages.exportBackup}</Button><Button variant="secondary" onClick={()=>backupInput.current?.click()}>{messages.restoreBackup}</Button></div><input className="sr-only" ref={backupInput} type="file" accept=".json,application/json" onChange={(e)=>{const file=e.target.files?.[0];if(!file)return;void readFile(file).then((text)=>{dispatch({type:"data/replace",data:parseBackup(text)});setMessage(messages.backupRestored);}).catch((error:unknown)=>{log("warn","backup.restore_failed",{kind:error instanceof Error?error.message:"unknown"});setMessage(error instanceof Error&&error.message==="file-too-large"?messages.fileTooLarge:messages.restoreFailed);});e.currentTarget.value="";}}/></Card><Card title={messages.courseCsv} eyebrow={messages.spreadsheetFriendly}>{data.courses.length===0?<p>{messages.createCourseCsvHint}</p>:<><label>{messages.course}<select value={courseId} onChange={(e)=>setCourseId(e.target.value)}>{data.courses.map((course)=><option key={course.id} value={course.id}>{course.name}</option>)}</select></label><div className="button-row"><Button onClick={()=>{const course=data.courses.find((item)=>item.id===courseId);if(course)downloadText(`${course.code||course.name}-grades.csv`,exportCourseCsv(course),"text/csv;charset=utf-8");}}>{messages.exportCsv}</Button><Button variant="secondary" onClick={()=>csvInput.current?.click()}>{messages.importAssignments}</Button></div><input className="sr-only" ref={csvInput} type="file" accept=".csv,text/csv" onChange={(e)=>{const file=e.target.files?.[0],course=data.courses.find((item)=>item.id===courseId);if(!file||!course)return;void readFile(file).then((text)=>{const imported=importAssignmentsCsv(text),categories=[...course.categories],assignments=[...course.assignments];for(const row of imported){let category=categories.find((item)=>item.name.toLowerCase()===row.categoryName.toLowerCase());if(!category){category={id:createId("category"),name:row.categoryName,weight:row.categoryWeight};categories.push(category);}const now=new Date().toISOString();assignments.push({id:createId("assignment"),categoryId:category.id,createdAt:now,updatedAt:now,...row.assignment});}const categoryIssues=validateCategories(categories,course.mode);if(categoryIssues.length>0)throw new Error("invalid-categories");const updated:Course={...course,categories,assignments,updatedAt:new Date().toISOString()};dispatch({type:"course/update",course:updated});setMessage(messages.importedAssignments(imported.length));}).catch((error:unknown)=>{log("warn","csv.import_failed",{kind:error instanceof Error?error.message:"unknown"});setMessage(error instanceof Error&&error.message==="file-too-large"?messages.fileTooLarge:messages.importCsvFailed);});e.currentTarget.value="";}}/></>}</Card></div><Card title={messages.privacyNote}><p>{messages.privacyNoteText}</p></Card></div>;}
