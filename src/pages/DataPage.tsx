import { useRef,useState } from "react";
import { createBackup,parseBackup } from "../data/backup";import { exportCourseCsv,importAssignmentsCsv } from "../data/csv";import type { Course } from "../domain/types";import { validateCategories } from "../domain/validation";import { useApp } from "../state/AppContext";import { createId } from "../utils/id";import { downloadText } from "../utils/download";import { Button } from "../components/Button";import { Card } from "../components/Card";
export function DataPage(){const{data,dispatch}=useApp();const[courseId,setCourseId]=useState(data.courses[0]?.id??""),[message,setMessage]=useState("");const backupInput=useRef<HTMLInputElement>(null),csvInput=useRef<HTMLInputElement>(null);async function readFile(file:File):Promise<string>{if(file.size>5_000_000)throw new Error("File is too large. Maximum size is 5 MB.");return file.text();}return <div className="page-stack"><div className="page-heading"><div><p className="eyebrow">Portable data</p><h1>Import & export</h1><p>Back up your local data or move assignments with CSV.</p></div></div>{message&&<div className="notice" role="status">{message}</div>}<div className="two-column"><Card title="Full backup" eyebrow="JSON"><p>Export courses, scales, and settings to a GradeCraft backup file.</p><div className="button-row"><Button onClick={()=>downloadText(`gradecraft-backup-${new Date().toISOString().slice(0,10)}.json`,createBackup(data),"application/json")}>Export backup</Button><Button variant="secondary" onClick={()=>backupInput.current?.click()}>Restore backup</Button></div><input className="sr-only" ref={backupInput} type="file" accept=".json,application/json" onChange={(e)=>{const file=e.target.files?.[0];if(!file)return;void readFile(file).then((text)=>{dispatch({type:"data/replace",data:parseBackup(text)});setMessage("Backup restored successfully.");}).catch((error:unknown)=>setMessage(`Could not restore backup: ${String(error)}`));e.currentTarget.value="";}}/></Card><Card title="Course CSV" eyebrow="Spreadsheet-friendly">{data.courses.length===0?<p>Create a course before using CSV transfer.</p>:<><label>Course<select value={courseId} onChange={(e)=>setCourseId(e.target.value)}>{data.courses.map((course)=><option key={course.id} value={course.id}>{course.name}</option>)}</select></label><div className="button-row"><Button onClick={()=>{const course=data.courses.find((item)=>item.id===courseId);if(course)downloadText(`${course.code||course.name}-grades.csv`,exportCourseCsv(course),"text/csv;charset=utf-8");}}>Export CSV</Button><Button variant="secondary" onClick={()=>csvInput.current?.click()}>Import assignments</Button></div><input className="sr-only" ref={csvInput} type="file" accept=".csv,text/csv" onChange={(e)=>{const file=e.target.files?.[0],course=data.courses.find((item)=>item.id===courseId);if(!file||!course)return;void readFile(file).then((text)=>{const imported=importAssignmentsCsv(text),categories=[...course.categories],assignments=[...course.assignments];for(const row of imported){let category=categories.find((item)=>item.name.toLowerCase()===row.categoryName.toLowerCase());if(!category){category={id:createId("category"),name:row.categoryName,weight:row.categoryWeight};categories.push(category);}const now=new Date().toISOString();assignments.push({id:createId("assignment"),categoryId:category.id,createdAt:now,updatedAt:now,...row.assignment});}const categoryIssues=validateCategories(categories,course.mode);if(categoryIssues.length>0)throw new Error(categoryIssues[0]?.message??"Imported categories are inconsistent.");const updated:Course={...course,categories,assignments,updatedAt:new Date().toISOString()};dispatch({type:"course/update",course:updated});setMessage(`Imported ${imported.length} assignment${imported.length===1?"":"s"}.`);}).catch((error:unknown)=>setMessage(`Could not import CSV: ${String(error)}`));e.currentTarget.value="";}}/></>}</Card></div><Card title="Privacy note"><p>Imports are parsed in your browser. GradeCraft does not upload these files to a server.</p></Card></div>;}
