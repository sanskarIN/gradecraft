import { readFile,readdir } from "node:fs/promises";import { basename,extname,join } from "node:path";
const roots=["."],ignored=new Set(["node_modules","dist","coverage","playwright-report","test-results",".git"]),textExtensions=new Set([".ts",".tsx",".js",".mjs",".json",".md",".yml",".yaml",".css",".html",".svg",".txt"]),patterns=[/-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----/,/\bgh[pousr]_[A-Za-z0-9_]{30,}\b/,/\bAIza[0-9A-Za-z_-]{35}\b/,/\bsk-[A-Za-z0-9]{20,}\b/,/\bAKIA[0-9A-Z]{16}\b/,/\bxox[baprs]-[A-Za-z0-9-]{10,}\b/];const files=[];
function isTextCandidate(path){const name=basename(path);return textExtensions.has(extname(name))||name===".env"||name.startsWith(".env.");}
async function walk(path){for(const entry of await readdir(path,{withFileTypes:true})){if(ignored.has(entry.name))continue;const full=join(path,entry.name);if(entry.isDirectory())await walk(full);else if(isTextCandidate(full))files.push(full);}}
for(const root of roots){try{await walk(root);}catch{}}
const findings=[];for(const file of files){if(file.endsWith("scripts/check-secrets.mjs"))continue;const text=await readFile(file,"utf8");for(const pattern of patterns)if(pattern.test(text))findings.push(`${file}: matched ${pattern.source}`);}if(findings.length){console.error(findings.join("\n"));process.exit(1);}console.log(`Secret-pattern scan OK for ${files.length} files.`);
