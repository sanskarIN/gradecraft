import { fireEvent,render,screen } from "@testing-library/react";import { describe,expect,it,vi } from "vitest";import { CourseForm } from "../src/components/CourseForm";import type { Course,GradeScaleProfile } from "../src/domain/types";
const scale:GradeScaleProfile={id:"s",name:"Standard",bands:[{id:"a",label:"A",minPercent:90,gpaPoints:4},{id:"f",label:"F",minPercent:0,gpaPoints:0}]};
const course:Course={id:"c",name:"Course",code:"C",color:"#000",creditHours:3,mode:"weighted",scaleId:"s",categories:[{id:"used",name:"Used",weight:50},{id:"free",name:"Free",weight:50}],assignments:[{id:"assignment",name:"Exam",categoryId:"used",score:8,maxScore:10,createdAt:"x",updatedAt:"x"}],createdAt:"x",updatedAt:"x"};
describe("CourseForm",()=>{it("prevents removing a category that still owns assignments",()=>{render(<CourseForm scales={[scale]} initial={course} onSave={vi.fn()}/>);const buttons=screen.getAllByRole("button",{name:"Remove"});expect(buttons[0]).toBeDisabled();expect(buttons[0]).toHaveAttribute("title",expect.stringMatching(/assignments/i));expect(buttons[1]).toBeEnabled();});it("trims category names before saving",()=>{const onSave=vi.fn();render(<CourseForm scales={[scale]} initial={course} onSave={onSave}/>);fireEvent.change(screen.getByLabelText("Category 2"),{target:{value:"  Free  "}});fireEvent.click(screen.getByRole("button",{name:"Save course"}));expect(onSave).toHaveBeenCalledTimes(1);const saved=onSave.mock.calls[0]?.[0] as Course;expect(saved.categories.find((category)=>category.id==="free")?.name).toBe("Free");});});
