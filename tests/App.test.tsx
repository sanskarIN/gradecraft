import { render,screen } from "@testing-library/react";import { afterEach,beforeEach,describe,expect,it,vi } from "vitest";import { App } from "../src/App";import { saveData } from "../src/data/storage";import { createDefaultData } from "../src/domain/defaults";import { AppProvider } from "../src/state/AppContext";
afterEach(()=>vi.restoreAllMocks());
describe("App",()=>{beforeEach(()=>{localStorage.clear();window.location.hash="#/dashboard";});it("renders an isolated onboarding dialog for first-run users",()=>{render(<AppProvider><App/></AppProvider>);expect(screen.getByRole("heading",{name:"Welcome to GradeCraft"})).toBeInTheDocument();expect(document.querySelector(".topbar")).toHaveAttribute("inert");expect(screen.getByRole("button",{name:"Continue"})).toHaveFocus();});it("exposes primary navigation and identifies the current page after onboarding",()=>{const data=createDefaultData();data.settings.onboardingComplete=true;saveData(data);render(<AppProvider><App/></AppProvider>);expect(screen.getByRole("navigation",{name:"Primary navigation"})).toBeInTheDocument();expect(screen.getByRole("link",{name:"Overview"})).toHaveAttribute("aria-current","page");expect(screen.getByRole("link",{name:"GPA"})).not.toHaveAttribute("aria-current");});it("warns when browser storage cannot save changes",async()=>{const data=createDefaultData();data.settings.onboardingComplete=true;saveData(data);vi.spyOn(Storage.prototype,"setItem").mockImplementation(()=>{throw new Error("blocked");});render(<AppProvider><App/></AppProvider>);expect(await screen.findByRole("alert")).toHaveTextContent(/cannot save changes/i);});});
