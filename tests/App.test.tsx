import { render,screen } from "@testing-library/react";import { beforeEach,describe,expect,it } from "vitest";import { App } from "../src/App";import { AppProvider } from "../src/state/AppContext";
describe("App",()=>{beforeEach(()=>{localStorage.clear();window.location.hash="#/dashboard";});it("renders onboarding for first-run users",()=>{render(<AppProvider><App/></AppProvider>);expect(screen.getByRole("heading",{name:"Welcome to GradeCraft"})).toBeInTheDocument();});it("exposes primary navigation",()=>{render(<AppProvider><App/></AppProvider>);expect(screen.getByRole("navigation",{name:"Primary navigation"})).toBeInTheDocument();});});
