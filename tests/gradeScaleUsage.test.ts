import { describe,expect,it } from "vitest";import { canDeleteScale,scaleDeletionBlockReason } from "../src/domain/gradeScaleUsage";import { createDefaultData } from "../src/domain/defaults";import type { Course,GradeScaleProfile } from "../src/domain/types";
const extraScale:GradeScaleProfile={id:"extra",name:"Extra",bands:[{id:"f",label:"F",minPercent:0,gpaPoints:0}]};
function course(scaleId:string):Course{return{id:"c",name:"Course",code:"",color:"#000",creditHours:3,mode:"points",scaleId,categories:[{id:"all",name:"All",weight:100}],assignments:[],createdAt:"x",updatedAt:"x"};}
describe("grading-scale deletion rules",()=>{it("protects the last remaining profile",()=>{const data=createDefaultData();expect(scaleDeletionBlockReason(data,data.gradeScales[0]!.id)).toBe("last-scale");});it("protects a profile referenced by a course",()=>{const data=createDefaultData(),scaleId=data.gradeScales[0]!.id,state={...data,gradeScales:[...data.gradeScales,extraScale],courses:[course(scaleId)]};expect(scaleDeletionBlockReason(state,scaleId)).toBe("in-use");});it("allows deleting an unused profile when another remains",()=>{const data=createDefaultData(),state={...data,gradeScales:[...data.gradeScales,extraScale]};expect(canDeleteScale(state,"extra")).toBe(true);});it("reports missing profile IDs",()=>{expect(scaleDeletionBlockReason(createDefaultData(),"missing")).toBe("missing");});});
