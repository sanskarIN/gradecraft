import { fireEvent,render,screen,waitFor } from "@testing-library/react";import { beforeEach,describe,expect,it } from "vitest";import { saveData } from "../src/data/storage";import { createDefaultData } from "../src/domain/defaults";import type { Course } from "../src/domain/types";import { CoursePage } from "../src/pages/CoursePage";import { AppProvider } from "../src/state/AppContext";
const course:Course={id:"course-1",name:"Science",code:"SCI",color:"#000000",creditHours:3,mode:"points",scaleId:"standard-4",categories:[{id:"tests",name:"Tests",weight:50},{id:"homework",name:"Homework",weight:50}],assignments:[{id:"exam",name:"Exam",categoryId:"tests",score:80,maxScore:100,createdAt:"2026-01-01T00:00:00.000Z",updatedAt:"2026-01-01T00:00:00.000Z"}],createdAt:"2026-01-01T00:00:00.000Z",updatedAt:"2026-01-01T00:00:00.000Z"};
describe("CoursePage",()=>{beforeEach(()=>localStorage.clear());it("drops an undo action when its original category is removed",async()=>{const data=createDefaultData();data.courses=[course];saveData(data);render(<AppProvider><CoursePage id="course-1"/></AppProvider>);fireEvent.click(screen.getByRole("button",{name:"Delete Exam"}));expect(screen.getByText(/Deleted “Exam”/)).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Edit course"}));const dialog=screen.getByRole("dialog",{name:"Edit course"});const removeButtons=withinDialogButtons(dialog,"Remove");fireEvent.click(removeButtons[0]!);fireEvent.click(screen.getByRole("button",{name:"Save course"}));await waitFor(()=>expect(screen.queryByText(/Deleted “Exam”/)).not.toBeInTheDocument());});});
function withinDialogButtons(dialog:HTMLElement,name:string):HTMLButtonElement[]{return Array.from(dialog.querySelectorAll("button")).filter((button):button is HTMLButtonElement=>button.textContent?.trim()===name);}
