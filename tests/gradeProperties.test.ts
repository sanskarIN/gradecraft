import { describe,expect,it } from "vitest";import { calculateCourseGrade } from "../src/domain/gradeMath";import type { Assignment,Course } from "../src/domain/types";
function randomFactory(seed=0x5eed1234){let state=seed>>>0;return()=>{state=(Math.imul(state,1664525)+1013904223)>>>0;return state/0x100000000;};}
function assignment(id:string,categoryId:string,score:number,maxScore:number):Assignment{return{id,name:`Assignment ${id}`,categoryId,score,maxScore,createdAt:"2026-01-01T00:00:00.000Z",updatedAt:"2026-01-01T00:00:00.000Z"};}
describe("grade calculation properties",()=>{it("keeps valid points-based grades within 0-100 percent",()=>{const random=randomFactory();for(let caseIndex=0;caseIndex<250;caseIndex+=1){const assignments=Array.from({length:1+Math.floor(random()*20)},(_,index)=>{const maxScore=1+Math.floor(random()*200),score=random()*maxScore;return assignment(`${caseIndex}-${index}`,"all",score,maxScore);}),course:Course={id:`course-${caseIndex}`,name:"Generated",code:"GEN",color:"#000000",creditHours:3,mode:"points",scaleId:"scale",categories:[{id:"all",name:"All",weight:100}],assignments,createdAt:"2026-01-01T00:00:00.000Z",updatedAt:"2026-01-01T00:00:00.000Z"},percent=calculateCourseGrade(course).percent;expect(percent).not.toBeNull();expect(percent!).toBeGreaterThanOrEqual(0);expect(percent!).toBeLessThanOrEqual(100);}});it("keeps valid weighted grades within 0-100 percent",()=>{const random=randomFactory(0xc0ffee);for(let caseIndex=0;caseIndex<250;caseIndex+=1){const firstWeight=Math.floor(random()*101),categories=[{id:"a",name:"A",weight:firstWeight},{id:"b",name:"B",weight:100-firstWeight}],assignments=categories.flatMap((category,categoryIndex)=>Array.from({length:1+Math.floor(random()*8)},(_,index)=>{const maxScore=1+Math.floor(random()*100),score=random()*maxScore;return assignment(`${caseIndex}-${categoryIndex}-${index}`,category.id,score,maxScore);})),course:Course={id:`weighted-${caseIndex}`,name:"Generated weighted",code:"WGT",color:"#000000",creditHours:3,mode:"weighted",scaleId:"scale",categories,assignments,createdAt:"2026-01-01T00:00:00.000Z",updatedAt:"2026-01-01T00:00:00.000Z"},percent=calculateCourseGrade(course).percent;expect(percent).not.toBeNull();expect(percent!).toBeGreaterThanOrEqual(0);expect(percent!).toBeLessThanOrEqual(100);}});});
