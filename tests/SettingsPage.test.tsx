import { fireEvent,render,screen,waitFor } from "@testing-library/react";import { beforeEach,describe,expect,it } from "vitest";import { SettingsPage } from "../src/pages/SettingsPage";import { AppProvider } from "../src/state/AppContext";
describe("SettingsPage locale",()=>{beforeEach(()=>{localStorage.clear();document.documentElement.lang="en";});it("switches the interface to Hindi and updates document language",async()=>{render(<AppProvider><SettingsPage/></AppProvider>);fireEvent.change(screen.getByLabelText("Language"),{target:{value:"hi"}});expect(screen.getByRole("heading",{name:"सेटिंग्स"})).toBeInTheDocument();await waitFor(()=>expect(document.documentElement.lang).toBe("hi"));});});
