import { render,screen } from "@testing-library/react";import { beforeEach,describe,expect,it } from "vitest";import { saveData } from "../src/data/storage";import { createDefaultData } from "../src/domain/defaults";import type { Course } from "../src/domain/types";import { WhatIfPage } from "../src/pages/WhatIfPage";import { AppProvider } from "../src/state/AppContext";
const course:Course={id:"course-1",name:"Mathematics",code:"MATH",color:"#000000",creditHours:3,mode:"points",scaleId:"standard-4",categories:[{id:"all",name:"All",weight:100}],assignments:[{id:"a1",name:"Quiz",categoryId:"all",score:8,maxScore:10,createdAt:"2026-01-01T00:00:00.000Z",updatedAt:"2026-01-01T00:00:00.000Z"}],createdAt:"2026-01-01T00:00:00.000Z",updatedAt:"2026-01-01T00:00:00.000Z"};
describe("WhatIfPage",()=>{beforeEach(()=>localStorage.clear());it("falls back to an available course when the route ID is stale",()=>{const data=createDefaultData();data.courses=[course];saveData(data);render(<AppProvider><WhatIfPage id="missing-course"/></AppProvider>);expect(screen.getByLabelText("Course")).toHaveValue("course-1");expect(screen.getByLabelText("What-if score for Quiz")).toBeInTheDocument();});});
