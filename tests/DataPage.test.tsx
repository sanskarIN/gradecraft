import { fireEvent,render,screen,waitFor } from "@testing-library/react";import { beforeEach,describe,expect,it,vi } from "vitest";
vi.mock("../src/data/encryptedBackup",()=>({createEncryptedBackup:vi.fn(async()=>'{"format":"gradecraft-encrypted-backup"}'),parseEncryptedBackup:vi.fn(),encryptedBackupPolicy:{minimumPassphraseLength:8,iterations:210000}}));
vi.mock("../src/utils/download",()=>({downloadText:vi.fn()}));
import { DataPage } from "../src/pages/DataPage";import { AppProvider } from "../src/state/AppContext";
describe("DataPage encrypted backup credentials",()=>{beforeEach(()=>{localStorage.clear();});it("clears passphrase fields after a successful encrypted export",async()=>{render(<AppProvider><DataPage/></AppProvider>);const passphrase=screen.getByLabelText("Backup passphrase"),confirm=screen.getByLabelText("Confirm passphrase");fireEvent.change(passphrase,{target:{value:"correct horse battery staple"}});fireEvent.change(confirm,{target:{value:"correct horse battery staple"}});fireEvent.click(screen.getByRole("button",{name:"Export encrypted backup"}));await waitFor(()=>expect(screen.getByText(/Encrypted backup created/)).toBeInTheDocument());expect(passphrase).toHaveValue("");expect(confirm).toHaveValue("");});});
