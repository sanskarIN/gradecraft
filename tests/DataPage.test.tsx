import { fireEvent,render,screen,waitFor } from "@testing-library/react";import { beforeEach,describe,expect,it } from "vitest";import { saveData } from "../src/data/storage";import { createDefaultData } from "../src/domain/defaults";import type { AppData,Course } from "../src/domain/types";import { DataPage } from "../src/pages/DataPage";import { AppProvider,useApp } from "../src/state/AppContext";
function course(id:string,name:string):Course{return{id,name,code:id.toUpperCase(),color:"#000000",creditHours:3,mode:"points",scaleId:"standard-4",categories:[{id:`${id}-all`,name:"All",weight:100}],assignments:[],createdAt:"2026-01-01T00:00:00.000Z",updatedAt:"2026-01-01T00:00:00.000Z"};}
function Harness({replacement}:{replacement:AppData}){const{dispatch}=useApp();return <><button onClick={()=>dispatch({type:"data/replace",data:replacement})}>Replace data</button><DataPage/></>;}
describe("DataPage",()=>{beforeEach(()=>localStorage.clear());it("moves CSV selection to a valid course after data replacement",async()=>{const initial=createDefaultData(),replacement=createDefaultData();initial.courses=[course("old","Old course")];replacement.courses=[course("new","New course")];saveData(initial);render(<AppProvider><Harness replacement={replacement}/></AppProvider>);expect(screen.getByLabelText("Course")).toHaveValue("old");fireEvent.click(screen.getByRole("button",{name:"Replace data"}));await waitFor(()=>expect(screen.getByLabelText("Course")).toHaveValue("new"));});});
