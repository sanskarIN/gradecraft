import { describe,expect,it } from "vitest";import { validateAssignment,validateCategories,validateGradeScale } from "../src/domain/validation";import { DEFAULT_SCALE } from "../src/domain/defaults";
describe("validation",()=>{it("requires at least one category",()=>{expect(validateCategories([],"points").some((issue)=>issue.field==="categories")).toBe(true);});it("requires weighted category totals to equal 100",()=>{expect(validateCategories([{id:"a",name:"A",weight:50}],"weighted")[0]?.message).toContain("100%");});it("rejects duplicate category names case-insensitively",()=>{const issues=validateCategories([{id:"a",name:"Tests",weight:50},{id:"b",name:" tests ",weight:50}],"weighted");expect(issues.some((issue)=>issue.message.includes("names must be unique"))).toBe(true);});it("rejects impossible scores",()=>{const issues=validateAssignment({name:"Exam",categoryId:"x",score:110,maxScore:100},{categories:[{id:"x",name:"Exams",weight:100}]});expect(issues.some((issue)=>issue.field==="score")).toBe(true);});it("accepts default scale",()=>{expect(validateGradeScale(DEFAULT_SCALE)).toEqual([]);});it("requires a grading-scale floor at zero percent",()=>{const scale={...DEFAULT_SCALE,id:"no-floor",bands:DEFAULT_SCALE.bands.map((band)=>({...band,minPercent:band.minPercent===0?10:band.minPercent}))};expect(validateGradeScale(scale).some((issue)=>issue.message.includes("starting at 0%"))).toBe(true);});it("rejects duplicate grading thresholds and band IDs",()=>{const scale={...DEFAULT_SCALE,id:"ambiguous",bands:[{...DEFAULT_SCALE.bands[0]!,id:"same",minPercent:90},{...DEFAULT_SCALE.bands[1]!,id:"same",minPercent:90},{...DEFAULT_SCALE.bands.at(-1)!}]};const issues=validateGradeScale(scale);expect(issues.some((issue)=>issue.message.includes("IDs must be unique"))).toBe(true);expect(issues.some((issue)=>issue.message.includes("percentages must be unique"))).toBe(true);});});
