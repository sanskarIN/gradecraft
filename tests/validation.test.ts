import { describe,expect,it } from "vitest";import { validateAssignment,validateCategories,validateGradeScale } from "../src/domain/validation";import { DEFAULT_SCALE } from "../src/domain/defaults";
describe("validation",()=>{it("requires at least one category",()=>{expect(validateCategories([],"points")[0]?.message).toContain("At least one category");});it("requires weighted category totals to equal 100",()=>{expect(validateCategories([{id:"a",name:"A",weight:50}],"weighted")[0]?.message).toContain("100%");});it("rejects impossible scores",()=>{const issues=validateAssignment({name:"Exam",categoryId:"x",score:110,maxScore:100},{categories:[{id:"x",name:"Exams",weight:100}]});expect(issues.some((issue)=>issue.field==="score")).toBe(true);});it("accepts default scale",()=>{expect(validateGradeScale(DEFAULT_SCALE)).toEqual([]);});it("rejects duplicate grading thresholds",()=>{const scale={...DEFAULT_SCALE,id:"custom",bands:[{...DEFAULT_SCALE.bands[0]!,id:"one",minPercent:90},{...DEFAULT_SCALE.bands[1]!,id:"two",minPercent:90},{id:"fallback",label:"F",minPercent:0,gpaPoints:0}]};expect(validateGradeScale(scale).some((issue)=>issue.message.includes("percentages must be unique"))).toBe(true);});it("rejects duplicate grade band identifiers",()=>{const scale={...DEFAULT_SCALE,id:"custom",bands:[{...DEFAULT_SCALE.bands[0]!,id:"same"},{...DEFAULT_SCALE.bands[1]!,id:"same"},{id:"fallback",label:"F",minPercent:0,gpaPoints:0}]};expect(validateGradeScale(scale).some((issue)=>issue.message.includes("IDs must be unique"))).toBe(true);});it("requires a zero-percent fallback band",()=>{const scale={...DEFAULT_SCALE,id:"custom",bands:DEFAULT_SCALE.bands.filter((band)=>band.minPercent>0)};expect(validateGradeScale(scale).some((issue)=>issue.message.includes("0% minimum"))).toBe(true);});});
