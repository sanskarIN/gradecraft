import { describe,expect,it } from "vitest";import { validateAssignment,validateCategories,validateGradeScale } from "../src/domain/validation";import { DEFAULT_SCALE } from "../src/domain/defaults";
describe("validation",()=>{it("requires weighted category totals to equal 100",()=>{expect(validateCategories([{id:"a",name:"A",weight:50}],"weighted")[0]?.message).toContain("100%");});it("rejects impossible scores",()=>{const issues=validateAssignment({name:"Exam",categoryId:"x",score:110,maxScore:100},{categories:[{id:"x",name:"Exams",weight:100}]});expect(issues.some((issue)=>issue.field==="score")).toBe(true);});it("accepts default scale",()=>{expect(validateGradeScale(DEFAULT_SCALE)).toEqual([]);});});
