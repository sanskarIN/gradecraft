import { describe,expect,it } from "vitest";import { createBackup,parseBackup } from "../src/data/backup";import { createDefaultData } from "../src/domain/defaults";import { UserFacingError } from "../src/errors/UserFacingError";
describe("backup",()=>{it("round trips app data",()=>{const data=createDefaultData("2026-01-01T00:00:00.000Z");expect(parseBackup(createBackup(data,"2026-01-02T00:00:00.000Z"))).toEqual(data);});it("rejects malformed JSON with a stable user-safe message",()=>{expect(()=>parseBackup("{broken")).toThrow(new UserFacingError("Backup is not valid JSON."));});it("rejects unknown formats",()=>{expect(()=>parseBackup('{"format":"other"}')).toThrow(/unsupported/i);});it("hides internal schema validation details for incompatible backup data",()=>{const invalid={format:"gradecraft-backup",formatVersion:1,data:{schemaVersion:99}};expect(()=>parseBackup(JSON.stringify(invalid))).toThrow("Backup data is invalid or incompatible with this version of GradeCraft.");});});
