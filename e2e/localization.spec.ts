import { expect,test } from "@playwright/test";import { completeOnboarding } from "./helpers";
test("language choice updates immediately and persists across reload",async({page})=>{await completeOnboarding(page);await page.getByRole("link",{name:"Settings"}).click();await page.getByLabel("Language").selectOption("hi");await expect(page.getByRole("heading",{name:"सेटिंग्स"})).toBeVisible();await expect(page.getByRole("link",{name:"अवलोकन"})).toBeVisible();await expect(page.locator("html")).toHaveAttribute("lang","hi");await page.reload();await expect(page.getByRole("heading",{name:"सेटिंग्स"})).toBeVisible();await expect(page.locator("html")).toHaveAttribute("lang","hi");});
