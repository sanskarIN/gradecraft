import { expect,test } from "@playwright/test";import { completeOnboarding,createWeightedCourse } from "./helpers";
test("student can review mapped third-party CSV headers before import",async({page})=>{await completeOnboarding(page);await createWeightedCourse(page);await page.getByRole("link",{name:"Import / Export"}).click();const csv=page.locator('input[type="file"][accept=".csv,text/csv"]');await csv.setInputFiles({name:"third-party-grades.csv",mimeType:"text/csv",buffer:Buffer.from("Group,Task,Marks,Total\nAssignments,Quiz 1,9,10")});await expect(page.getByRole("heading",{name:"Map CSV columns"})).toBeVisible();const importButton=page.getByRole("button",{name:"Import mapped CSV"});await expect(importButton).toBeEnabled();await importButton.click();await expect(page.getByText("Imported 1 assignment.")).toBeVisible();await page.getByRole("link",{name:"Overview"}).click();await page.getByRole("heading",{name:"Algebra"}).click();await expect(page.getByRole("cell",{name:"Quiz 1"})).toBeVisible();});
